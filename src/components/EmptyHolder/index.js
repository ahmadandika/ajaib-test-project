import { Box, Typography } from '@mui/material';
import React from 'react';

const EmptyHolder = () => (
  <Box textAlign="center" sx={{ p: 20 }}>
    <Typography variant="subtitle1">Empty Data</Typography>
  </Box>
);

export default EmptyHolder;
